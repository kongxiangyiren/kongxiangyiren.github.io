这个文件的内容会被忽略，Jekyll 只看它**存在与否**。

存在的唯一理由：GitHub Pages 默认用 Jekyll 处理站点，而 Jekyll 会**跳过所有以 `_` 开头的
文件与目录**。本项目的构建产物里有这类文件：

    assets/_slug_-<hash>.js      ← 文章详情页
    assets/_name_-<hash>.js      ← 标签/分类详情页（两个）
    assets/_...all_-<hash>.js    ← 404 页

少了这个文件，上述 4 个 chunk 不会被发布 → 详情页、标签/分类详情页、404 页全部崩。

（文件名里的 `_` 是 vue-router 文件路由对动态段 `[slug]` / `[name]` / `[...all]` 的转写，
不是我们起的名字，无法改。所以只能靠 `.nojekyll` 关掉 Jekyll 处理。）
